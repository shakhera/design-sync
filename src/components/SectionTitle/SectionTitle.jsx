import React from 'react';

const SectionTitle = ({heading}) => {
    return (
        <div>
            <h2 className='text-2xl font-bold ml-4'>{heading}</h2>
        </div>
    );
};

export default SectionTitle;